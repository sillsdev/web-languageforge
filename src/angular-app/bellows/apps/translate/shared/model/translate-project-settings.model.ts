import { ProjectSettings } from '../../../../shared/model/project-settings.model';

export class TranslateProjectSettings extends ProjectSettings {
  interfaceConfig: any;
  lastSyncedDate: string;
}
