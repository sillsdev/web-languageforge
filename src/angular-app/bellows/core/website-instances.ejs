<% // This EJS template will be processed by Gulp with values taken from WebsiteInstances.json -%>
// This file is generated by a gulp task from website-instances.ejs and WebsiteInstances.json. Do not directly modify!

export const websiteInstances = {
<% var all = [ scriptureforge, languageforge ];
for (var j = 0; j < all.length; j++) {
  for (var i = 0; i < all[j].length; i++) {
  host = all[j][i];
  if (host.__comment) { %>
  // <%- host.__comment %>
  <%_ } -%>
  '<%- host.domain %>': '<%- host.releaseStage %>',
<% }};%>
};
