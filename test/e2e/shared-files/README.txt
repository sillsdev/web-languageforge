This directory is shared between the app-for-e2e container and the host
machine running the E2E tests. It is used for transferring files (such
as media files from test/common) to the container without needing to go
through an HTTP upload step.
